#include "header.h"

int main()
{
    // JOB a = NULL;
    // a->ArrivalTime = 3;
    // a->DurationTime = 3;
    // strcpy(a->JobContent, "aaa");
    // printf("%d", a->ArrivalTime);
    scheduler();
    return 0;
}
